// Tunable physics and layout constants for Space Dash
export const GRAVITY = 0.6;          // pixels per frame² pulling player down
export const JUMP_VELOCITY = -12;    // upward velocity on tap
export const OBSTACLE_SPEED = 4;     // pixels per frame obstacles move left
export const OBSTACLE_GAP = 180;     // vertical gap between top/bottom obstacles (px)
export const OBSTACLE_WIDTH = 60;    // obstacle column width (px)
export const OBSTACLE_INTERVAL = 90; // frames between new obstacles spawning
export const PLAYER_SIZE = 40;       // player sprite width/height (px)
export const PLAYER_X = 80;          // fixed horizontal position of player (px from left)
export const FRAME_RATE = 60;        // target frames per second
